# Convenience pin-set for `pip install -r requirements.txt`.
# pyproject.toml is the source of truth — prefer `pip install -e ".[all]"`.
#
# Core only: the engine and the entire test suite run on these alone, offline.
pandas>=2.1
numpy>=1.26
scikit-learn>=1.3
pyyaml>=6.0

# Optional stacks, installed here for a one-command demo environment.
# Drop these for a lean install: pip install -e "."
faiss-cpu>=1.7.4          # [embeddings] dense semantic tier
sentence-transformers>=2.5 # [embeddings] production embedder
anthropic>=0.40           # [llm]        production adjudicator
streamlit>=1.35           # [app]        dashboard

# Tooling
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
